# Copy this file to .env and set it to your Oracle Cloud VM's public IP.
# Used only by the FTP challenge (passive mode masquerade address), so
# that anonymous FTP LIST/RETR works correctly for players connecting
# from outside the host.
PUBLIC_IP=203.0.113.10
